import { describe,it,expect } from "vitest"; import { destinationEta,resolveDestination,canTransition } from "../src/journey-lifecycle.js";
const stop={stationCode:"NDLS",stationName:"New Delhi",scheduledArrival:"2026-08-11T10:00:00+05:30",scheduledDeparture:null,expectedArrival:"2026-08-11T10:30:00+05:30",expectedDeparture:null,actualArrival:null,actualDeparture:null,delayMinutes:30,status:"upcoming"};
describe("journey lifecycle",()=>{it("uses expected arrival as an absolute instant",()=>expect(destinationEta(stop).toISOString()).toBe("2026-08-11T05:00:00.000Z"));it("validates route identity by station code",()=>expect(resolveDestination({trainNumber:"12919",journeyDate:"2026-08-11",status:"running",currentStation:null,currentStationCode:null,previousStation:null,nextStation:null,delayMinutes:null,latitude:null,longitude:null,stops:[stop]},"NDLS").stationName).toBe("New Delhi"));it("allows only documented state transitions",()=>{expect(canTransition("scheduled","cancelled")).toBe(true);expect(canTransition("completed","cancelled")).toBe(false)})});
