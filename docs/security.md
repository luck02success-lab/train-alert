# Security
Implement authentication before exposing journeys, ownership checks on every journey/device query, schema validation, rate limits at the edge/API, and generic safe errors. Do not log credentials or device tokens. Android must never contain RailRadar or FCM server credentials.
